import './HeroSection.css';

const HeroSection = () => {
  return (
    <div className="hero_section">
      <h2 className="heroTitle">Welcome to My Website.</h2>
      <p className="heroText">My React Journey Start Here.</p>
      <button className="heroBtn">Purchase Products</button>
    </div>
  );
};

export default HeroSection;
